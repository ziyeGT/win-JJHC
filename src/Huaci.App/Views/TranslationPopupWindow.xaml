<Window x:Class="Huaci.App.Views.TranslationPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="330" MinWidth="280" MaxWidth="390"
        SizeToContent="Height" MaxHeight="280"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        ShowActivated="False" Topmost="True" Background="Transparent"
        AllowsTransparency="True" SnapsToDevicePixels="True" UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <Style x:Key="ToastButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="26" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#32FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#4AFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToastToggleButtonStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Width" Value="26" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#32FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#554F6BED" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#AA9FAEFF" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="True" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Chrome" Property="Background" Value="#704F6BED" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="7" />
            <Setter Property="MinWidth" Value="7" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Width="7" Background="Transparent">
                            <Track x:Name="PART_Track" Margin="1,0"
                                   IsDirectionReversed="True"
                                   Minimum="{TemplateBinding Minimum}"
                                   Maximum="{TemplateBinding Maximum}"
                                   Value="{TemplateBinding Value}"
                                   ViewportSize="{TemplateBinding ViewportSize}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                                  Focusable="False" Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb MinHeight="28" Focusable="False">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="#70FFFFFF" CornerRadius="3" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                                  Focusable="False" Opacity="0" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder" Margin="0" Padding="12,10,9,11"
            Background="#E63A3D44" BorderThickness="0" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="10,-3,-2,0">
                    <Button x:Name="CopyButton" Style="{StaticResource ToastButtonStyle}"
                            IsEnabled="False" ToolTip="复制译文" AutomationProperties.Name="复制译文"
                            Click="CopyButton_OnClick">
                        <Viewbox Width="12" Height="12">
                            <Path Fill="White" Data="M6,2 H18 A2,2 0 0 1 20,4 V16 H18 V4 H6 Z M4,6 H15 A2,2 0 0 1 17,8 V19 A2,2 0 0 1 15,21 H4 A2,2 0 0 1 2,19 V8 A2,2 0 0 1 4,6 M4,8 V19 H15 V8 Z" />
                        </Viewbox>
                    </Button>
                    <ToggleButton x:Name="PinButton" Style="{StaticResource ToastToggleButtonStyle}"
                                  Margin="2,0,0,0" ToolTip="固定窗口"
                                  AutomationProperties.Name="固定窗口"
                                  Checked="PinButton_OnChecked" Unchecked="PinButton_OnUnchecked">
                        <Viewbox Width="13" Height="13">
                            <Path Fill="White" Data="M7,2 H17 V4 H16 V10 L19,13 V15 H13 V22 H11 V15 H5 V13 L8,10 V4 H7 Z M10,4 V11 L8,13 H16 L14,11 V4 Z" />
                        </Viewbox>
                    </ToggleButton>
                    <Button Style="{StaticResource ToastButtonStyle}" Margin="2,0,0,0"
                            ToolTip="关闭" AutomationProperties.Name="关闭"
                            Click="CloseButton_OnClick">
                        <Viewbox Width="11" Height="11">
                            <Path Fill="White" Data="M3,2 L10,9 L17,2 L19,4 L12,11 L19,18 L17,20 L10,13 L3,20 L1,18 L8,11 L1,4 Z" />
                        </Viewbox>
                    </Button>
                </StackPanel>

                <Border x:Name="OriginBadge"
                        DockPanel.Dock="Left"
                        Margin="0,1,7,0"
                        Padding="6,2"
                        VerticalAlignment="Top"
                        Background="#28FFFFFF"
                        CornerRadius="5"
                        Visibility="Collapsed">
                    <TextBlock x:Name="OriginTextBlock"
                               FontSize="8.5"
                               Foreground="#D9FFFFFF" />
                </Border>

                <TextBlock x:Name="SourceTextBlock" MaxHeight="34" Margin="1,2,0,0"
                           TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                           Foreground="#BFFFFFFF" FontSize="11" LineHeight="17"
                           AutomationProperties.Name="划选原文" />
            </DockPanel>

            <Grid Grid.Row="1" Margin="1,9,3,0">
                <ScrollViewer x:Name="ResultScrollViewer" MaxHeight="200"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              PanningMode="VerticalOnly" Focusable="False">
                    <TextBlock x:Name="ResultTextBlock" TextWrapping="Wrap"
                               Foreground="White" FontSize="14" LineHeight="21"
                               AutomationProperties.Name="翻译结果"
                               AutomationProperties.LiveSetting="Polite" />
                </ScrollViewer>
                <StackPanel x:Name="LoadingPanel" Orientation="Horizontal" VerticalAlignment="Center">
                    <ProgressBar Width="46" Height="3" IsIndeterminate="True" VerticalAlignment="Center" />
                    <TextBlock Margin="9,0,0,0" Text="翻译中…" Foreground="#D9FFFFFF"
                               FontSize="11.5" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
